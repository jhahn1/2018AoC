const assert = require("assert");

const sumOfItsParts = require("./sum-of-its-parts");
const sumOfItsParts2 = require("./sum-of-its-parts2");

describe("Day 7: Sum of its Parts", () => {
//   it("should determine the correct order of steps in the instructions", () => {
//     const instructions = `Step C must be finished before step A can begin.
//         Step C must be finished before step F can begin.
//         Step A must be finished before step B can begin.
//         Step A must be finished before step D can begin.
//         Step B must be finished before step E can begin.
//         Step D must be finished before step E can begin.
//         Step F must be finished before step E can begin.`;

//     assert.equal(sumOfItsParts2(instructions), 15);
//   });

});
